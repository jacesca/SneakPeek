# sort dictionaries
# dict(sorted(distance_degree_criteria.items(), key=lambda item: item[1], reverse=True))

# get the key with the highest value
# max(dict(degree_centrality), key=dict(degree_centrality).get)